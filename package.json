{
  "name": "hippocare-ai",
  "version": "1.0.0",
  "description": "HippoCare AI - Precision Radiotherapy Hippocampal Segmentation",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server.js",
    "client": "cd client && npm start",
    "install-all": "npm install",
    "install-client": "cd client && npm install",
    "postinstall": "cd client && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "sharp": "^0.32.6",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

